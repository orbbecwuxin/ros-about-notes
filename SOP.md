# 中文翻译 SOP（标准作业流程）

**目的**：把 ROS 官方文档的每一段英文，逐段翻译成中文，并记录自己的理解，最终发布到自己的 GitHub Pages。

**适用范围**：`index.html` 中所有「📝 译文」框对应的英文段落。

---

## 一、每次翻译的四个步骤（每段重复）

| 步骤 | 做什么 | 填在哪 |
|------|--------|--------|
| ① 读 | 阅读译文框上方的英文段落 | — |
| ② 译 | 把英文翻译成中文，填进译文框 | 「📝 译文：」后面 |
| ③ 解 | 用一句话写下你对这段的理解（**可选**） | 「💡 理解：」后面 |
| ④ 存 | 保存 → 推送 → 网页自动更新 | — |

```
英文原文段落……
┌───────────────────────────────┐
│ 📝 译文：（在这里填中文翻译）      │
│ 💡 理解：（用一句话写下你的理解）    │
└───────────────────────────────┘
```

---

## 二、填写规范

1. **只改文字，不碰标签**：只替换 `（在此填写中文翻译）`、`（用一句话写下你的理解，可选填）` 这些占位文字，`<div>`、`<span>` 等代码一概不动。
2. **术语保留英文**：ROS、Node、Topic、Service、Parameter 等专有名词可保留英文，或用「中文（英文）」形式，如“节点（Node）”。
3. **译文忠实原文**：不添加原文没有的内容；个别地方可加方括号注释，如 `[译者注：……]`。
4. **理解用自己话写**：一两句即可，不求长，写你真正理解到的点。
5. **不翻译的段落**：把整个译文框（`<div class="my-translation"> ... </div>`）整块删除。

---

## 三、填写入口（二选一）

### 方式 A：GitHub 网页直接改（偶尔填几条）
1. 打开 https://github.com/orbbecwuxin/ros-about-notes
2. 进入 `index.html` → 点 ✏️ 编辑
3. `Ctrl+F` 搜索 `在此填写中文翻译` 或 `用一句话写下你的理解`
4. 替换占位文字 → **Commit changes**
5. 等 1~2 分钟，访问 https://orbbecwuxin.github.io/ros-about-notes/ 查看

### 方式 B：本地编辑（批量翻译推荐）
1. 用 VS Code 打开 `~/ros-notes-site/index.html`
2. 逐段填「译文」「理解」
3. 本地双击 `index.html` 预览
4. 推送：
```bash
cd ~/ros-notes-site
git add -A
git commit -m "翻译第 X 段"
git push
```

---

## 四、完成与发布检查

- [ ] 每个要翻译的段落都有译文
- [ ] 标签未被破坏（页面能正常打开、样式正常）
- [ ] 顶部署名与 CC BY 4.0 说明仍在
- [ ] `git push` 成功，网页 1~2 分钟后更新

---

## 五、质量控制（可选）

- 术语统一：遇到不熟悉的术语先在官方 Glossary 查一次再定译名。
- 复查：翻译完几段后，回读一遍中文是否通顺、是否漏译。
- 版本：每次推送 commit message 写清楚改了哪几段，方便回溯。

*本 SOP 与页面顶部「翻译 SOP」说明保持一致。*
